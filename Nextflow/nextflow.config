process {
    executor = 'local'
    container = 'pierrejeangouze/sra_toolkit:latest'
}

docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
    registry = 'docker.io'
}

params {
    // (optionnel) chemin de sortie
    outdir = './results'
}
